import React from 'react';
import { Input } from 'shared/ui';

const MainPage = () => {
    return (
        <div>
            <p>MainPage</p>
            <Input placeholder='Поиск'/>
        </div>
    );
};

export default MainPage;